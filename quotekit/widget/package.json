{
  "name": "@quotekit/widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "watch": "vite build --watch",
    "test": "npm run build && node test/smoke.mjs"
  },
  "dependencies": {
    "expr-eval": "^2.0.2"
  },
  "devDependencies": {
    "jsdom": "^24.1.0",
    "vite": "^6.0.0"
  }
}
